# Build-script approval for the @earendil-works/pi-ai peer graph (mirrors
# pi-napkin/pi-steering): esbuild's binary is needed by tsx, genai's
# postinstall by its JS runtime; protobufjs works without its postinstall.
allowBuilds:
  '@google/genai': true
  esbuild: true
  protobufjs: false
